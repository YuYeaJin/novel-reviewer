# 이야기가 진행되면서 캐릭터별 캐릭터의 특징이 변화 없는지 평가하는 노드

def analyze_characters(text: str) -> dict:
    """
    캐릭터성 유지, 일관성, 개성 등을 평가.
    """
    result = {
        "character_consistency": None,
        "character_depth": None,
        "relationships_notes": [],
    }
    return result
